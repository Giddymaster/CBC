Django>=6.0
djangorestframework
django-filter
django-cors-headers
celery
redis
requests
python-dotenv
reportlab
anthropic
Pillow
whitenoise
# Production database + server. Installed here too so `pip install -r` on a
# server is one command; the SQLite dev default does not need psycopg.
psycopg[binary]
gunicorn
